Lab 1
- Toured the image in SAOImageDS9
- Setup the basic framework in VSCode for importing and reading the data

Lab 2
Estimate the number of galaxies in the image and compare with histogram count to evaluate confidence.

Take small regions of the image for faster analysis (and for total number estimation)

Compare with existing results.

Lab 3
- equation 2 in the lab script is important for the motivation on the whole lab
- One value of pixel is much greater than the rest. This is due to parts of the image at which none of the photos cover, so they have been blocked in with a uniform pixel value (assumably the pixel value which was the mean of the first photo)
- The peak in the histogram is due to the overlay of multiple images - the edges of the resultant mosaic image have a constant background value where images don't overlap
- Demonstrator recommendation: Use scipy.optimize.curve_fit instead of norm.fit for Gaussian fitting (provides more control and better error estimates)

Background Statistics:
- Image size: 4611 × 2570 pixels = 11,850,270 total pixels
- Background mean: μ = 3421.3 counts
- Background standard deviation: σ = 20.1 counts
- Distribution: Approximately Gaussian

Gaussian distribution:
- 1σ: 16% of pixels
- 2σ: 2.5% of pixels
- 3σ: 0.15% of pixels
- 5σ: 0.00006% of pixels

Lab 6
- The source detection is counting single pixels, however the image is taken from Earth, in which the atmosphere interferes. It spreads the light 1 arcsecond which is equivelant to 6 pixels.
- We added additional thresholds: The brightest pixel has to be brighter than 5 sigma, the pixels imediatly around the source have to be brighter than 4 sigma, and the pixels immediatly around that need to be greater than 3 sigma. This reduces the sources detected in the range data=data[1050:1700, 390:1300], from 576 to 292, which we believe is more accurate.
- Deleted the code in Section 5.2.6 which excludes the edge effect pixels. I did this because it would be more simple to crop the area of the image instead.
- Demonstrator suggested that 3 levels of filtering is overkill (in the sense that its too much computer power)
- As seen in "First Overlay plot", large stars are getting detected as multiple. To counteract this, we have the idea to: find  brightest pixel, expand until reaches pixels below threshold. Then filter for size and only accept ones greater than a certain value.
- Demonstrator mentioned that masking (ignoring objects already detecdted), might not be working


Lab 7
- Start at initial pixel and bloom out, compare average brightness to threshold, and stop once average is below certain threshold. This will ensure large objects are not counted more than once.
- We did this and counted 287 objects.
- We noticed we were failing to detect sources because the average of the pixels would be above the threshold, for a slightly greater radius than the true radius. This would also cause near by stars to be made False, so they dont get detected. [commit 7-1]
- We thought we could have a ring expanding instead, that was the high value pixels in the centre of the object would not interfere as much with when the radius stops expanding. We got 487 sources detected. We think this may be overcounting. Due to the ring expanding in a circle, and sources that arnt a circle would end up be counted multiple times. [commit 7-2]

Lab 8
- need to compare results to our model (the equation), and other peoples results.
- Demonstrator said we need to make our detection algorithm more sophisticated, this could be by adding an additional rule for detecting, or adjusting what we already have. We want confidence that our results represent whats in the image.
- For higher magnitudes (lower brightness), we have a threshold for how low magnitudes we detect, so we dont want to include points we dont detect in out fit. For lower magnitude brighter stars, we want to do a weighted fit as they have greater error bars.
- Should use a local background/histogram instead.
- Problem to solve: elipse is being counted as multiple objects.
- Solution: Instead of masking out the circle of the radius of the object. Mask out a circle of radius = radius_detected * k, where k = 1.5 - 2. [commit 8-1].
- This still detected elipses as multiple sources, despite, visually, the minor radius of the elipse * 1.5 being greater than the major radius. This suggests that the radius is too small. So I decided to decrease the threshold of the ring. I decreased the ring threshold from 3 sigma -> 2 sigma. This improved visual results and decreased sources detected from 487 -> 261. This slighly improved the gradient from 0.239 -> 0.247.
- Noticed some objects werent getting detected, decreased the seed detection threshold from 4 sigma -> 3 sigma, which increased the count from 261 -> 393. I noticed it was still failing to detect some sources.
- To add the local backgrounds, I am going to detect the brightest source based off the whole area, then finding the local background, and then checking the seded ocmpared to the local background. i have not done this yet. [commit 8-2]
- Changed the code, so it calculated the median instead of the mean. This was to stop the radius being greater than its true value. When the sources overlap, it can cause the mean to be higher than it should be, but the median can fix this. [commit 8-3]

Lab 9
- made the magnitude fit a weighted fit, and it decreased the gradient.
- Some objects were sill being double counted. I think this is because the boolean map is used to mask out for the seed detection, but is not masked out for the ring expansion. I changede it so it uses the masked_data when expanding the ring. This improved visual results. Decreased the number of detections 344 -> 292. (seed_threshold = mu + 3 * sigma, ring_threshold = mu + 2.5 * sigma) [commit 9-1]
- Need to make simulated image with known values, to test code. Could calculate uncerainty of code from results? Add overlapping sources, and elipse shape sources in simulation.
- What is the zero point uncertainty used for?
- How to convert magnitude uncertainties (from code detection  method uncertainty, and zero point magnitude uncertainty) into number of sources detected uncertainty. 
- The magnitude plot should use non normalised y axis to help understanding, but for final plot should use same axis' that proffesional figures use.
- Our magnitude values are shifted (they are too high), this is due to our flux not being accurate, due to a gain, and not accounting for time.
- We will plot a non-accumalitive magnitude plot to help out understanding. 
- We should have a minimum radius value. We can skip to the minimum radius value, check that the median or the area of the circle is greater than the threshold, if yes, expand a ring from there. <- I changed the minimum radius to 6, however i have not done the skip to 6 and do circle area thing yet, which would make the code run faster. This decreased the sourcxes detected 292 -> 63 for sigma_seed = 3, sigma_ring = 2.
- We need to account for exposure time and gain in the magnitude values (eg on the number counts per magnitude bin plot)
- We should still detect high magnitude objects (really small and dim) but then store the radius as 6.
- Use the number counts histogram to justify exluding data on the cumulative number counts plot. Low magnitude data on histogram is not great because of counting statistics (only detect around 1 object)
- You dont need to implement everything, if its faster to quanity the uncertainty something has, than coding to mitigate that, then just quanify the uncertainty.
- Its not that sources that we detect that are under 6 pixels in radius dont count. Its that sources that we detect to have a radius < 6, we should still count them, but when calculating the flux, the radius should be measured from a minimum of 6 pixels, because its spread to 6 pixels, but its jsut hidden in the background. Basically the radius is probably greater that what we are detecting now.
- Maybe we could compare one ring standard deviation to the next iteration ring standard deviation, and say when it doesnt change anymore its reached background.
- [commit 9-2]

- I changed the code, the minimam radius for detection is 1. The radius used for flux finding will be the radius detected + 6 pixels.
- I changed it again. The minimum radius for detection is 1. All found radius' will be multiplied by factor k = 1.5 to include whole source. And if a radius is still smaller than 6 pixels after that, it gets changed to 6 pixels radius.
- Could run Gaussian code on image with removed sources, to see if it fits perfectly.